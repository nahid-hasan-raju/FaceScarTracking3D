# ─────────────────────────────────────────────────────────────────────────────
#  INPUT  —  uncomment ONE mode, comment the rest
# ─────────────────────────────────────────────────────────────────────────────

# ── Batch mode (all patients / all timepoints) ────────────────────────────────
# dataset: "D:/NahidW/Dataset/face_burn_dataset"

# ── Batch mode (one patient only) ────────────────────────────────────────────
# dataset:   "D:/NahidW/Dataset/face_burn_dataset"
# patient:   "PAT01"

# ── Batch mode (one patient, one timepoint) ───────────────────────────────────
# dataset:    "D:/NahidW/Dataset/face_burn_dataset"
# patient:    "PAT01"
# timepoint:  "D00"

# ── Single scan subfolder ─────────────────────────────────────────────────────
# scandir: "D:/NahidW/Dataset/face_burn_dataset/PAT01/D00/PAT01_D00_A"

# ─────────────────────────────────────────────────────────────────────────────
#  MODEL — uncomment ONE block, comment the rest
# ─────────────────────────────────────────────────────────────────────────────

# ── UNet++ ────────────────────────────────────────────────────────────────────
# model:  unetpp
# ckpt:   "D:/NahidW/Coding/4.face_burn_segmentation_models/checkpoints/unetpp/best.pth"

# ── SegFormer ─────────────────────────────────────────────────────────────────
# model:  segformer
# ckpt:   "D:/NahidW/Coding/4.face_burn_segmentation_models/checkpoints/segformer"

# ── MedSAM ───────────────────────────────────────────────────────────────────
# model:      medsam
# ckpt:       "D:/NahidW/Coding/4.face_burn_segmentation_models/checkpoints/medsam/best.pth"
# base_ckpt:  "D:/NahidW/Coding/4.face_burn_segmentation_models/checkpoints/medsam/medsam_vit_b.pth"

# ── SAM2 ─────────────────────────────────────────────────────────────────────
model:      sam2
ckpt:       "D:/NahidW/Coding/4.face_burn_segmentation_models/checkpoints/sam2/best.pth"
base_ckpt:  "D:/NahidW/Coding/4.face_burn_segmentation_models/checkpoints/sam2/sam2_hiera_large.pt"
sam2_repo:  "D:/NahidW/Coding/4.face_burn_segmentation_models/segment-anything-2"

# ─────────────────────────────────────────────────────────────────────────────
#  OUTPUT
# ─────────────────────────────────────────────────────────────────────────────
# threshold:  0.92      # burn/normal cutoff — only affects unetpp and segformer
fine_yaw:   0.0       # degrees — adjust if face isn't centred in the PLY
save_mask:  false      # also save 2D binary mask PNG
show:       false     # open Open3D 3D viewer after processing

# THRESHOLD GUIDE: SAM2=0.65  MedSAM=0.55  UNet++=0.6  SegFormer=0.7

threshold: 0.80   # general/default -- used for any patient NOT listed below

patient_thresholds:
  PAT01: 0.92
  PAT02: 0.75
  PAT03: 0.89
  PAT04: 0.80
  PAT05: 0.90
  PAT06: 0.92
  PAT07: 0.90
  PAT08: 0.65
  PAT09: 0.70
  PAT10: 0.82
  PAT11: 0.75
  PAT12: 0.80
  PAT13: 0.80
  PAT14: 0.80
  PAT15: 0.80
  PAT16: 0.80